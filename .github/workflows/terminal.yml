name: CI

on:
  push:
    branches:
      - master
jobs:
  test:
    runs-on: ubuntu-latest
    #runs-on: self-hosted
    steps:
    - uses: actions/checkout@v2
    - name: Style checks
      run: ghaction_script.sh
